spring:
  application:
    name: user
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:80/
#      discovery:
#        enabled: true
#        service-id: config-server

  rabbitmq:
    host: 47.114.132.140
    port: 5672
    username: admin
    password: admin

server:
  port: 8881

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
